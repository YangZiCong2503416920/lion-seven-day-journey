# 《一头醒狮的七日远行》升级蓝图 · 设计总监版

> 目标竞赛：附件4 「粤易创新」创意作品展示活动 · 类别 **H5页面**（提交**页面截图** + **网络链接**，JPEG / RGB / 100DPI / <10MB）
> 设计定位：**让评委"截图张张有内容、有冲击力"，让真人"有冲动转发"**。
> 本文是创意+合规蓝图；`index.html / verify_page.py / assets` 的实际改动由 frontend-engineer 执行，judge-qa 按第7节验收。

---

## 0. 一句话结论

升级不是"堆动画"，而是先补上**命门**（整页截图不得空白），再把**首屏冲击力、每章节视觉锚点、分享/传播闭环**做足。二者缺一，作品在"页面截图评审"这一关就会全军覆没。

---

## 1. 命门：scroll-reveal 导致整页截图空白（必须先解决）

### 1.1 证据
- `index.html` 中 `.reveal{opacity:0;transform:translateY(28px)}`，靠 IntersectionObserver 在**滚动进视口后**才加 `.visible`。
- `verify_page.py` 用 `page.screenshot(full_page=True)` 且**不滚动**即截图；改页加载后立即截整页，下方内容从未进过视口 → 一直停在 `opacity:0`。
- 实测 `preview-mobile.png`（390×5854）：hero 之后到结尾是一大片空白米色；`preview-desktop.png`（1440×5179）：hero 内容几乎空白，只剩渐变底。
- **后果**：附件4 要求 H5 以**页面截图**评审。评委若直接对整页截图，作品会显得"几乎空白"→ 内容表现力归零，"眼前一亮"无从谈起。

### 1.2 根因
> 任何"基于滚动/视口才把内容显示出来"的机制，在"不滚动的整页截图"里都会把内容藏掉。

### 1.3 修复方案（推荐：内容默认可见，动效永不隐藏内容）

**改 `index.html` 的 CSS（第223–224行）**，把 `.reveal` 从"默认隐藏"改为"默认可见"：

```css
/* 命门修复：内容默认完全可见，绝不因滚动/视口而 opacity:0 —— 整页截图永不空白 */
.reveal{opacity:1;transform:none}
```

**保留/强化为"一次性入场，但不改 opacity"**，让真人仍有"眼前一亮"的进场感（把下面的块加到同区域，`prefers-reduced-motion` 下自动禁用）：

```css
@media (prefers-reduced-motion:no-preference){
  .reveal{animation:rise .7s cubic-bezier(.2,.75,.25,1) both}
  .hero .reveal{animation-delay:.06s}
  @keyframes rise{
    from{transform:translateY(22px);filter:blur(3px)}
    to{transform:none;filter:blur(0)}
  }
}
```
> 关键：入场只用 `transform` 位移 + `blur`，**全程 `opacity:1`**。截图在动画任何瞬间拍到，内容都完整、不透明、可读，只是略微下移/轻微模糊。0.7s 动画在 `networkidle` 前基本走完。

**同步删掉/弱化 JS 里的滚动隐藏**（第446行）。可改为"纯装饰性"的 transform 微效果（可选），但**严禁再引入 opacity 隐藏**：
```js
// 可选：保留一个"入场即显示所有"的兜底，保证任何工具/任何时机截图都不空
window.addEventListener('load',()=>{document.querySelectorAll('.reveal').forEach(el=>el.classList.add('visible'))});
```
（若保留 IntersectionObserver，仅用于触发 `transform` 微妙效果，不要用它控制 `opacity`。）

### 1.4 为什么这样改最稳
- 评审工具不可控：可能不滚动、可能禁用 JS、可能截图时机很早。**默认可见**对以上全部免疫。
- `prefers-reduced-motion` 下入场动画关闭，内容仍是 `opacity:1`。
- 保留进场动效 → 真人浏览仍有质感，不牺牲"眼前一亮"。

### 1.5 验证方式
改完后运行 `python3 verify_page.py`，用图像工具查看 `preview-mobile.png` / `preview-desktop.png`：**整页应无大段空白，每个章节内容全部可见**。再把 viewport 分别设 390×844、1440×900 各截一张首屏，确保 hero 饱满。

---

## 2. 视觉"眼前一亮"升级（在现有 红/金/青 体系上强化）

### 2.1 设计语言定位（供全页统一）
一句话：**"岭南红 × 科创青 × 醒狮金"**，是一条从"非遗根脉"走向"青年创造"的色彩叙事。
- 现有 `--red:#c4263b`、`--red-deep:#7e1629`、`--gold:#dca23f`、`--gold-light:#f4d38a`、`--jade:#4d9ba0`、`--wine:#170b11` 体系很好，**保留为骨架**。
- 新增一支**品牌青/金**来自队徽（见2.4），用于 CTA、进度、印章、签名等"点睛"元素，避免全页只有红。

### 2.2 字体层级（稳 > 花）
- 标题用现有 `Georgia / Noto Serif SC / Songti SC`（衬线，庄重有文化感）——保留。
- 建议正文/标注再加一档**更清晰的疏密**：大标题 `clamp()` 上限可再拉高（首屏 h1 到 ~clamp(60px,9vw,132px)），增强截图冲击力；段落 `letter-spacing` 微调至不挤。
- 关键数字（7天/26课时/25+孩子）拉大做"数据锚点"，截图更抓人。

### 2.3 首屏 hero（最重要的一张截图）
现状：深色底 + 居中星形发光 + 醒狮/纸飞机/火箭构图 + 少量数据。**已不错，但可把冲击力拉满**：
- 把 hero 主画面（`cover_bg_1920x1080.jpg`）适度放大/提高对比，让星形光芒成为视觉中心。
- h1 首行字重与行距调紧，`em`（"七日远行"）用金色 + 轻微光晕；标题区加**逐字入场**（transform/blur，不用 opacity，见1.3 同款）。
- 顶部增加**队徽**（`final_logo.png`，详见2.4）与"启智润心实践队"字样，强化学校身份。
- hero 底部数据带（7/26/25+）改成**金色描边高亮**，作为截图第一眼的"成果数字"。

### 2.4 品牌落地：队徽 & 校名（当前空缺）
- 队徽 `设计/团队文化/final_logo.png`（蓝环、金三星、青山、绿苗、橙阳、蓝基座）目前**完全没有**出现在页面里。
- 建议：在 topbar 左侧（`brand-seal` 处）和小屏版、以及 `.final` 结尾落款处，**放一个去底/圆形的队徽**；配色上把队徽的**蓝+金**作为"青年建功/学校身份"的点睛色，与醒狮红形成"一热一稳"的平衡，既不忘本又显专业。
- `.final` 落款明确写"广东建设职业技术学院 · 未来智能学院 · 启智润心实践队"（已有），补"指导老师：___"与作品二维码（见第4节，需真人补）。

### 2.5 每个章节都要"截图即饱满"
评委可能截任意一段，因此**每一屏都不能空**：
- 每章顶部已用 `.overline` + 大标题 + 短句，保持。
- 给每章的正文/图片都预留**明确定位 + 背景纹理**，确保无 `reveal` 隐藏、无空白。
- 图片用真实素材（见第4节真人替换）；AI 生成图加标注（见4.2）。

---

## 3. 交互升级（提升转发/传播力 + 眼前一亮）

> 原则：交互不只是"好玩"，更要**产生"可带走、可转发"的东西**。

### 3.1 现有可玩点增强
- **点醒醒狮**（hero liōn）：点击加**金色粒子爆 + 鼓点声 + 盖章"已醒"**，比现在的 toast 更有仪式感。
- **漆扇**：展开纹样后，加一句"这一把扇子，只属于完成它的那个孩子"，并**可长按保存为图**（生成一张孩子作品卡）。
- **水火箭**：发射后增加**火焰尾迹 + 屏幕微震 + 落下一句孩子的愿望/问题**（把"solve problem"落到具体）。

### 3.2 新增"传得出去"的传播引擎（重点）
- **"给孩子折一架纸飞机"**：结尾/某章放一个输入框，用户写一句对孩子的祝福 → 生成一架**纸飞机 + 你的落款**的分享卡片 → 一键保存/分享。
- 这是**用户生成内容（UGC）**，是自然转发/传播力最强的机制；同时呼应正文"纸飞机"意象（冯达飞"飞出去"）。
- 实现：Canvas 生成卡片图，`navigator.share` 分享文字，`clipboard` 兜底复制。

### 3.3 结尾分享闭环增强
- 现在的"分享这段远行"是通用分享。改成**引导生成专属传播卡**（3.2），分享文案带作品名+"粤易创新"tag，提升被点开的概率。
- 二维码 + 网络链接放 `.final`（需真人补），扫码即达作品。

### 3.4 声音/氛围
- 保留现有 sound-toggle；正式版可接一段**醒狮鼓点**（WebAudio 合成或用 `设计/viedo` 里的现场鼓声），作为"环境音"。截图不影响。

---

## 4. 内容与合规（附件4 逐条对齐）

### 4.1 主题覆盖（已达标，保留）
岭南非遗（醒狮/漆扇/扎染）· 红色南粤（冯达飞）· 科创（纸飞机/水火箭/AI绘本）· 青年建功（把专业写在乡土上）→ 导向鲜明、鼓舞人心。✅

### 4.2 AI 生成标注（有合规风险，必须处理）
- hero 主图 `cover_bg_1920x1080.jpg` 右下角有 **"AI生成"水印**。
- 附件4 + 作品要求强调"立场、观点、原创性"；README 也明确"若用 AI 生成形象需标'基于史实的艺术化演绎'"。
- **处理**：在页面（如 `.final` 或图片 figcaption）加一行"本页部分插图由 AI 辅助生成，用于艺术化演绎；历史与人物内容以可核验资料为准。" 既诚实又规避风险；历史人物（冯达飞）**不要虚构原话**，用学生真实舞台表达或可核验资料。

### 4.3 真实性
- 数据（7天/26课时/25+孩子）需与真实核对（真人）。建议把数据做成**可改的集中变量**或不放易出错的具体数。
- 图片/原话/视频用真实素材替换 `assets/` 示例图（真人）。

### 4.4 署名/链接/二维码
- 学校 + 学院 + 实践队署名（已有）。补**指导老师姓名**、**作品网络链接、二维码**（真人）。
- 推荐表/汇总表需学校盖章（真人）。

### 4.5 分享 meta（提升被打开与转发的"第一眼"）
- `<head>` 加 Open Graph / 微信分享 meta：`og:title / og:description / og:image`（用 hero 图）+ 微信 `description`。这样链接被转发时卡片好看，直接提升传播力。
- 保持 `theme-color` 等现有 meta。

---

## 5. 截图交付规范（让评委"截图即眼前一亮的证据"）

> 附件4 硬要求：JPEG / RGB 色彩模式 / 100DPI / 文件 <10MB，另附网络链接。

### 5.1 建议产出一整套提交截图（每张都达标）
| 顺序 | 内容 | 建议尺寸 |
|---|---|---|
| 01 | 首屏 hero（最抓眼球） | 移动 1080×1920 或 竖屏整屏 |
| 02 | 序言大标语 | 桌面 1440×900 |
| 03–06 | 七日旅程 每章一张关键帧（图文并茂） | 桌面 / 移动 |
| 07 | 互动时刻（醒狮/漆扇/水火箭点亮后） | 移动 |
| 08 | 作品档案 + 结尾 + 学校署名 + 二维码 | 移动 |

### 5.2 截图脚本升级建议（`verify_page.py` 或新脚本）
- **边滚动边截**（`full_page` 之前先把页面滚到底，或逐屏截图合并），配合 1.3 修复后即使不滚动也**不再空白**。
- 导出时统一：`JPEG, quality ~85`、`dpi=(100,100)`（RGB 默认）；检查 `<10MB`。
- 复现"截图评审"视角：用与评委一致的比例（移动竖屏 + 桌面横屏）各出图，人工看图确认"无空白、每屏饱满"。

---

## 6. 落地任务清单（给 frontend-engineer）

> 优先级：**P0=命门必须先做**；P1=眼前一亮；P2=锦上添花。

### P0（阻断级，先做）
1. **修 scroll-reveal 截图空白**：`index.html` 第223–224行 `.reveal` 改 `opacity:1;transform:none`；把隐藏改为 transform+blur 一次性入场（见1.3 代码）；第446行 JS 加"load 后 reveal-all"兜底（可选）；删除/弱化基于 `opacity` 的隐藏逻辑。

### P1（眼前一亮 + 传播）
2. **hero 冲击力**：标题字号/间距微调、金色光晕、逐字入场（transform/blur）；数据带金色高亮。
3. **队徽落地**：topbar + `.final` 放队徽，引入蓝金点缀色（`--brand-blue`、`--brand-gold`）。
4. **"给孩子折一架纸飞机"**：输入一句祝福 → Canvas 生成分享卡片 → 分享/保存；结尾引导改为生成传播卡。
5. **互动增强**：醒狮粒子+盖章、水火箭火焰+微震+孩子愿望、漆扇可保存成卡。
6. **分享 meta**：`<head>` 加 `og:*` + 微信分享字段；`og:image` 用 hero 图。

### P2（合规完善）
7. AI 生成标注行（4.2）；真实素材替换占位（真人）；`verify_page.py` 升级为"滚动+100DPI JPEG"截图脚本（5.2）。

---

## 7. 给 judge-qa 的验收点

- [ ] 整页截图（移动/桌面）**无任何大段空白**，每章内容全部可见。
- [ ] 首屏 hero 饱满、有冲击力；数据锚点清晰。
- [ ] 队徽/校名/学院/实践队落款出现。
- [ ] AI 生成内容有标注；冯达飞无虚构原话。
- [ ] 附上"作品说明 ≤300字"（推荐表字段）+ 主题四要素覆盖说明。
- [ ] meta 分享字段存在；`og:image` 有效。
- [ ] 截图符合 JPEG / RGB / 100DPI / <10MB；网络链接可访问。

---

## 8. 风险与取舍
- **取舍**：为保截图安全，牺牲了"滚动才逐屏浮现"的强滚动叙事。用"内容默认可见 + 一次性进场动效"补足质感，这是面对"页面截图评审"的**正确取舍**。
- **风险**：AI 素材标注若遗漏，可能被指"原创性/真实性"。务必落地 4.2。
- **依赖真人**：学校盖章、二维码、网络链接、真实素材、作者/指导老师名单、数据核对——工程师无法代做，需队长协调真人限时补齐（截止 2026-08-24 24:00）。

---

*本蓝图由 art-director（设计总监）产出，供 frontend-engineer 落地 & judge-qa 验收。*
